
/* 
  * unordered_set<int> st; // * Stores in Unsorted Order, uses a HASH TABLE
  * set<int> st; // * Stores in Sorted order, uses a Balanced Binary Search Tree like AVL tree or RED-Black Tree
  * Set and map usually gurantees a return of element in O(logn) time
  * and Unordered_map and set takes O(1) approx and at max logn
*/

/*
  * Some common methods
  * ump[i]
  * ump.end() iterator indicating the END Of unordered_map
  * erase()
  * clear()
  * find()
  * Explore - cpp reference
*/

